function palindrome(str) {
  
  newStr = str.toLowerCase().replace(/\W+|\_/g, '');
  
  if ( newStr === newStr.split('').reverse().join('') ) {
    
    return true;
    
  } else {
    
    return false;
    
  }
}